#include "main.h"

/**
 * rev_string - is a function that reverses a string
 *
 * @s: is the variable that is going to be reversed
 *
 * Return: Nothing
 */

void rev_string(char *s)
{

	char i;

	s = "My School"

	for (s = 0; s != "\0"; s++)
	{
		_putchar(s);
	}

	 _putchar("\n");

	 *s = i;

	for (i = "\0"; i >= 0; i++)
	{
		_putchar(i);
	}
	_putchar(i);
}

